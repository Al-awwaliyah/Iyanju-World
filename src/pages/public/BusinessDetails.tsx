import { Mail, MapPin, MessageCircle, Phone, Store } from "lucide-react";
import { Link, useParams } from "react-router-dom";
import PageContainer from "@/components/layout/PageContainer";
import ProductGrid from "@/components/marketplace/ProductGrid";
import { useMarketplaceBusiness, useMarketplaceProducts } from "@/hooks/useMarketplace";
export default function BusinessDetails(){const {slug=""}=useParams();const {business,loading:bl}=useMarketplaceBusiness(slug);const {products,loading:pl}=useMarketplaceProducts({limit:100});const storeProducts=business?products.filter(p=>p.businessId===business.id):[];if(bl)return <PageContainer><div className="py-16 text-center text-sm text-slate-500">Loading store…</div></PageContainer>;if(!business)return <PageContainer><div className="py-16 text-center"><h1 className="text-2xl font-bold">Store not found</h1></div></PageContainer>;return <div className="bg-slate-50"><section className="bg-[#082A63] text-white"><PageContainer className="py-8"><div className="flex flex-col gap-5 sm:flex-row sm:items-center"><div className="flex h-24 w-24 items-center justify-center overflow-hidden rounded-2xl bg-white/10">{business.logoUrl?<img src={business.logoUrl} alt="" className="h-full w-full object-cover"/>:<Store className="h-10 w-10 text-white/50"/>}</div><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h1 className="text-3xl font-black">{business.name}</h1>{business.verified&&<span className="rounded-full bg-emerald-500/20 px-2 py-1 text-xs">Verified</span>}</div><p className="mt-2 max-w-2xl text-sm text-blue-100">{business.description||"Marketplace business store"}</p><div className="mt-3 flex flex-wrap gap-4 text-xs text-blue-100">{(business.city||business.state)&&<span className="inline-flex items-center gap-1"><MapPin className="h-4 w-4"/>{[business.city,business.state].filter(Boolean).join(", ")}</span>}<span>{business.productCount} published product{business.productCount===1?"":"s"}</span></div></div></div><div className="mt-6 flex flex-wrap gap-2">{business.phone&&<a className="inline-flex items-center gap-2 rounded-lg bg-white px-4 py-2 text-sm font-semibold text-[#082A63]" href={`tel:${business.phone}`}><Phone className="h-4 w-4"/> Call</a>}{business.whatsapp&&<a className="inline-flex items-center gap-2 rounded-lg bg-emerald-500 px-4 py-2 text-sm font-semibold text-white" href={`https://wa.me/${business.whatsapp.replace(/\D/g,"")}`}><MessageCircle className="h-4 w-4"/> WhatsApp</a>}{business.email&&<a className="inline-flex items-center gap-2 rounded-lg border border-white/30 px-4 py-2 text-sm font-semibold" href={`mailto:${business.email}`}><Mail className="h-4 w-4"/> Email</a>}</div></PageContainer></section><PageContainer className="py-10"><h2 className="mb-5 text-2xl font-black">Products from {business.name}</h2><ProductGrid loading={pl} products={storeProducts.map(toCard)} columns={5}/></PageContainer></div>}
function toCard(p:any){return {id:p.id,name:p.name,slug:p.slug,businessName:p.businessName,businessSlug:p.businessSlug,price:p.price,compareAtPrice:p.compareAtPrice,imageUrl:p.imageUrl,categoryName:p.categoryName,stock:p.stock,available:p.available,featured:p.featured};}
