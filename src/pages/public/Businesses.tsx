import { useMemo, useState } from "react";
import { Search, Store } from "lucide-react";
import PageContainer from "@/components/layout/PageContainer";
import BusinessCard from "@/components/marketplace/BusinessCard";
import { useMarketplaceBusinesses } from "@/hooks/useMarketplace";
export default function Businesses(){const {businesses,loading}=useMarketplaceBusinesses();const [search,setSearch]=useState("");const [verified,setVerified]=useState(false);const [open,setOpen]=useState(false);const filtered=useMemo(()=>businesses.filter(b=>(!search||`${b.name} ${b.description??""} ${b.city??""} ${b.state??""}`.toLowerCase().includes(search.toLowerCase()))&&(!verified||b.verified)&&(!open||b.open)&&b.productCount>0),[businesses,search,verified,open]);return <PageContainer><div className="space-y-6 pb-16"><section className="rounded-2xl bg-[#082A63] p-6 text-white sm:p-8"><div className="flex items-center gap-3"><Store className="h-8 w-8 text-[#F4B400]"/><div><h1 className="text-3xl font-black">Business Stores</h1><p className="mt-1 text-sm text-blue-100">Only active businesses with published products are shown.</p></div></div><div className="mt-6 relative"><Search className="absolute left-3 top-1/2 h-5 w-5 -translate-y-1/2 text-slate-400"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search stores or locations…" className="h-12 w-full rounded-xl border-0 bg-white pl-11 pr-4 text-slate-900 outline-none"/></div></section><div className="flex flex-wrap gap-3"><label className="flex items-center gap-2 rounded-lg border bg-white px-3 py-2 text-sm"><input type="checkbox" checked={verified} onChange={e=>setVerified(e.target.checked)}/> Verified</label><label className="flex items-center gap-2 rounded-lg border bg-white px-3 py-2 text-sm"><input type="checkbox" checked={open} onChange={e=>setOpen(e.target.checked)}/> Open</label><span className="ml-auto self-center text-sm text-slate-500">{filtered.length} store{filtered.length===1?"":"s"}</span></div>{loading?<div className="py-12 text-center text-sm text-slate-500">Loading stores…</div>:filtered.length===0?<div className="rounded-2xl border border-dashed bg-white px-6 py-14 text-center text-sm text-slate-500">No active stores with published products match your search.</div>:<div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{filtered.map(b=><BusinessCard key={b.id} business={{id:b.id,name:b.name,slug:b.slug,description:b.description,logoUrl:b.logoUrl,coverUrl:b.coverUrl,city:b.city,state:b.state,verified:b.verified,open:b.open,productCount:b.productCount}}/>)}</div>}</div></PageContainer>}
